import styled from "styled-components";

const Logo = styled.img`
  display: block;

  flex-shrink: 0;

  width: auto;
  max-width: 128px;
  max-height: 128px;

  object-fit: scale-down;
  object-position: end;
`;

export default Logo;
